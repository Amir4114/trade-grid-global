import SettingsWorkspace from "@/components/settings/SettingsWorkspace";

export default function SupplierSettingsPage() {
  return (
    <SettingsWorkspace
      role="supplier"
      title="Settings"
      description="Manage your account, company profile, verification, and notification preferences."
    />
  );
}
