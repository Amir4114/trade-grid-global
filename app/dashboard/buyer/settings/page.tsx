import SettingsWorkspace from "@/components/settings/SettingsWorkspace";

export default function BuyerSettingsPage() {
  return (
    <SettingsWorkspace
      role="buyer"
      title="Settings"
      description="Manage your account, organization profile, verification, and notification preferences."
    />
  );
}
